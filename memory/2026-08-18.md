# 2026-08-18/19 — opsiton-lead

## Tur 1 (03:14 UTC): PM-RESTART — PR #122 merge, #128 eskalasyon, #99 seçimi
(Detay: [SONUÇ] PM-lead'e 03:15 UTC'de gönderildi; özet: PR #122 merged cf69b6b, stale-base riski tree-level doğrulandı; #128 org ayarı Rik/Vahit'e; #99 rate limiting seçildi.)

## Tur 2 (10:2x UTC): Stale test onarımı (P2, "onar" kararı) — TAMAM
- **PR #133** açıldı (fix/stale-tests, 6 commit, tests-only, 6 dosya +271/-922).
- `440 passed/99 skipped` → **`464 passed/5 skipped/8 xfailed`**. 82 skip kaldırıldı.
- **Bulgu:** WAL+write lock (issue #51) hiç master'a merge edilmemiş (commits edd1617/3009bfd unmerged feature branch'lerde). 5 test `xfail(strict=False)` ile gap tracking; #51 gelince otomatik xpass.
- test_api.py module-level skip "routes not yet implemented" YALANMIS: 9 route mevcut; tenant-scoped `/api/v1/*` yüzeyine karşı yeniden yazıldı (20 aktif test), eski v0 route testleri silindi.
- Tenant isolation flake: process-wide tenant registry → benzersiz tenant id'leri (8f642c3).
- CI: 19/20 yeşil; tek fail **PR Labels** (bilinen org 403, #128 ile aynı GITHUB_TOKEN label permission).
- [SONUÇ] PM-lead'e gönderildi.

## Dersler
- `git diff master..branch` stale-base'te yanıltır; PR içeriği `gh api pulls/N` files + head ref tree ile doğrulanmalı.
- Module-level `pytestmark = skip` dosyayı "yeşil" gösterir — skip sayısı CI'da görünür olmalı (opsiyonel: skip threshold check'i).
- Tenant context registry process-wide (module-level) → testler benzersiz tenant id kullanmalı.
- Commit öncesi `uv run ruff check --fix` + `ruff format`.
- Workspace root = repo checkout olduğu için git clean bootstrap dosyalarını silebilir (WorkspaceVanishedError) — Rik kalıcı çözüm için not düştü; ben `git stash -u` ile güvenli yol kullandım.

## Bekleyen
- PR #133 merge onayı (Rik/Vahit/PM) — test-only, risk sıfır.
- #128 org ayarı (Rik/Vahit) → staging deploy yeşil.
- #124 (CLI no-op), #123 (OpenAPI) sıradaki işler.
- Öneri: #51 (WAL+write lock) re-implementation P2 issue'su.
- Stale-base PR koruması (PM backlog'a ekleyecek, P2).

## Tur 3 (22:12 UTC): PR #133 MERGED
- Squash merge → **a57654c** (master HEAD), branch silindi.
- uv.lock gerekçesi PR body'sine eklendi (exceptiongroup marker 3.13→3.11 + psycopg resolution = metadata re-resolution, runtime değişmedi).
- 8 xfail master'da doğrulandı (5 sqlite + 1 ui class-level). Lokal yeşil: 464/5/8.
- [SONUÇ] PM-lead'e gönderildi. Sıradaki: #124 (CLI no-op) — talimat bekleniyor.
